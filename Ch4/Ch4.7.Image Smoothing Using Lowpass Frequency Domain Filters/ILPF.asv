function [reject_image] = ILPF(image,D0)
%UNTITLED2 이 함수의 요약 설명 위치
%   자세한 설명 위치
mask = IdealMaskGenerate(size(image),D0);
image_fft = fftshift(fft2(image));
reject_image_fft = image_fft .* mask;
reject_image = real(ifft2(ifftshift(reject_image_fft)));

imshow(reject_image)
end

